# Local backup of the uncompressed source video — do not upload (exceeds GitHub 100MB limit)
video_source_backup.mp4

# ffmpeg two-pass logs
ffmpeg2pass-*.log
ffmpeg2pass-*.log.mbtree

# paper source
tex

# raw demo source clips (web-ready copies live in assets/demos/)
real_world_supplements